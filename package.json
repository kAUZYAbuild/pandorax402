{
  "name": "pandoraX402",
  "private": true,
  "workspaces": ["packages/*", "services/*", "apps/*"],
  "scripts": {
    "i": "yarn --silent",
    "build": "yarn workspaces run build",
    "dev:db": "docker compose -f infra/docker-compose.yml up -d postgres redis",
    "db:push": "yarn workspace gateway-402 prisma db push && yarn workspace jobs prisma db push && yarn workspace conversations prisma db push",
    "dev:gateway": "yarn workspace gateway-402 dev",
    "dev:jobs": "yarn workspace jobs dev",
    "dev:merchants": "yarn workspace merchants dev",
    "dev:merchant-bot": "yarn workspace merchant-bot dev",
    "dev:agent": "yarn workspace agent dev",
    "dev:agent-pay": "yarn workspace agent server",
    "dev:agent-bot": "yarn workspace agent-bot dev",
    "dev:convos": "yarn workspace conversations dev",
    "dev:dash": "yarn workspace dashboard dev -p 3000",
    "dev": "run-p dev:db dev:gateway dev:jobs dev:merchants dev:merchant-bot dev:convos dev:dash dev:agent dev:agent-pay dev:agent-bot"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  }
}
